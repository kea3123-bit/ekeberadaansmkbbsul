export async function sendMail(to:string|string[],subject:string,text:string,html?:string){const key=process.env.RESEND_API_KEY,from=process.env.MAIL_FROM;if(!key||!from)return{ok:false,skipped:true};const recipients=(Array.isArray(to)?to:[to]).filter(Boolean);if(!recipients.length)return{ok:false,skipped:true};const r=await fetch('https://api.resend.com/emails',{method:'POST',headers:{authorization:`Bearer ${key}`,'content-type':'application/json'},body:JSON.stringify({from,to:recipients,subject,text,html:html||`<pre>${text.replace(/[&<>]/g,m=>m==='&'?'&amp;':m==='<'?'&lt;':'&gt;')}</pre>`})});if(!r.ok)return{ok:false,error:await r.text()};return{ok:true}}
